# 格式标签清单

把整个 `Localize/en` 与 `LLC_zh-CN` 扫了一遍，标签名**穷举**如下。
`limbus/fmtcode.py` 里是硬编码的同一份表，不做模式推断。
**发现新标签就往那两个元组里加一条，不要改成正则通配。**

## 一、真标签

| 标签 | 全量 EN | 全量 ZH | 分类 | 说明 |
|---|---:|---:|---|---|
| `<color=#xxxxxx>` `</color>` | 5337 | 4952 | **强** | 着色。游戏里存在畸形闭合 `</color=#xxx>`，照抄别改 |
| `<style=…>` | 8175 | 8118 | **强** | 样式预设 |
| `<mark=…>` | 771 | 784 | **强** | 高亮 |
| `<noparse>` | 118 | 124 | **强** | 禁止解析 |
| `<i>` `</i>` | 1834 | 394 | 软 | 斜体。官方中文常直接丢掉 —— 我们不学，照抄 |
| `<b>` `</b>` | 972 | 961 | 软 | 粗体 |
| `<u>` `</u>` | 1311 | 963 | 软 | 下划线 |
| `<s>` `</s>` | 66 | 66 | 软 | 删除线 |
| `<size=…>` `</size>` | 362 | 825 | 软 | 字号。中文侧常主动加 |
| `<voffset=…>` | 0 | 134 | 软 | 垂直偏移。纯中文侧排版用 |
| `<width=…>` | 0 | 1 | 软 | 宽度 |
| `<ruby=…>` `</ruby>` | 80 | 308 | 软 | 注音。中文侧常主动加 |
| `<link=…>` | 462 | 102 | 软 | 链接 |
| `<font=…>` | 15 | 2 | 软 | 字体 |

**强** = 数量与原文不一致（多或少）→ `batch_merge.py` 整批拒绝。

**软** = 官方译者会按中文习惯增删，所以拿来判断哪些标签属于排版类。
**但我们自己的译文不许增删**：丢一个是硬错误，自己加一个也是硬错误。

唯一放行的新增是**良秀的注音**：

```
EN: Yoshihide...?
ZH: <ruby=Yoshihide>良秀</ruby>……？
```

即 `<ruby=Yoshihide>` 及其配对的 `</ruby>`，硬编码在 `fmtcode.ALLOWED_ADDED_TAGS`。
其余任何新增（`<size>` `<voffset>` `<i>` `<ruby=别的>`）一律拒绝合批。

## 二、占位符（全部形态，穷举）

```
{}  {0} {1} {2} {3} {4} {5} {6}  {0:F1}  {n}
{amount} {stack} {target} {targets} {affectedTarget}
{conditions} {conditional} {reward} {buffName} {waveNum}
{hpAmount} {mpAmount}
```

全部按**强标记**处理，个数和编号都要保留。

## 三、乱码序列

`#&#(!@&$`、`%#@&**?!` 这种。官方原样保留（个别地方会重排符号）。按强标记处理。

整条文本全是乱码（`#&*()@%&!)$&8%&*%#%#!`）时不含英文单词，扫描阶段就跳过了，不会进队列。

## 四、**不是标签的括号**（最容易搞错）

### `<尖括号>` = 心声 / 通讯台词的引号

tier0 里 2913 处。**里面的话要翻译，尖括号保留。**

```
EN: <I hereby confirm today's close of business for the Sinners.>
ZH: <我批准罪人结束工作。>

EN: <Uh...>        ZH: <呃……>
EN: <Sinclair…>    ZH: <辛克莱……>
```

游戏还用它包游戏术语：`<Clash>`、`<Bloodfiend>`、`<Resonance>`、`<Staggered>`、
`<Panic>`、`<Sanity>` 等，ZH 侧全部为 0 —— 都翻掉了。

**所以标签名必须穷举**：用 `<[^>]+>` 通配会把这些台词当标签保护起来，译者就翻不动了。

### `[方括号]` = 广播 / 系统音 / 日志 / 音效的标注

tier0 里 154 处。**里面的话要翻译，方括号保留。**

```
EN: [INITIATING SITE BURIAL.]              ZH: [设施掩埋开始。]
EN: [ENERGY TRANSFER IS 85% COMPLETE.]     ZH: [能量传输，已完成85%。]
EN: [23:02:11]\n[…Hour 23, Minute 2, Faust speaking…]
ZH: [23:02:11]\n[……二十三点零二分，浮士德……]
EN: <color=#9eada4><size=60%><i>[Activating]</i></size></color>
ZH: <color=#9eada4><size=60%><i>[启动声]</i></size></color>
```

注意最后一条：**剧情里连 `[Activating]` 这种单个英文词也是要翻的音效标注。**

只有机制文本（Skills / Passives / BattleKeywords）里的 `[Sinking]` `[Burst]` `[WhenUse]`
才是关键词 ID，保留英文不译 —— 游戏运行时按 ID 去关键词表换显示名。
这个区别由 `config.KEYWORD_BRACKET_SCOPES` 控制，tier0 全部不在其中。

## 五、换行

原文的 `\n` 是刻意排版，默认保留。官方译者偶尔会把两行并成一行（中文更顺时），
所以只警告不拦。**不能输出字面 `\n` 两个字符** —— 游戏会原样显示 `\n` 而不换行。

## 六、规则的验证

拿 tier0 全部 **91584 条官方 EN→ZH 真实翻译对**跑校验：

| | 条数 | 占比 |
|---|---:|---:|
| 强标记误报 | 19 | 0.021% |
| 排版标签增删 | 971 | 1.1% |

（那 971 条是**官方**译文的增删情况，用来判断哪些标签属于排版类。
我们自己的译文按更严的标准走：排版标签既不许丢也不许加，只有良秀注音一个例外。）

那 19 条误报本身都有问题，不是规则错：

- `ProjectGS.json` 8 条 —— 旧 GPT 脚本留下的双语污染，如 `"第 {0} 天\n Day {0}"`，占位符翻倍
- `PC13B.json` 1 条 —— EN 缺 `</color>` 闭合，ZH 补上了（ZH 更正确）
- `S205A.json` 1 条 —— 官方把乱码 `%#@&**?!` 重排成了 `#@$@#$！！！`
- 其余 9 条同类

> `StoryData/ProjectGS.json` 里有旧机翻残留的双语行，哪天想清理可以从这里入手。
